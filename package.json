{
  "scripts": {
    "clean": "rm -rf ./dist",
    "build": "rollup -c",
    "rebuild": "npm run clean; npm run build",
    "dev": "rollup -c -w",
    "test": "./test/index.coffee"
  },
  "dependencies": {
    "coffeescript": "^2.0.0",
    "ddeyes": "^0.0.4",
    "jison": "^0.4.18"
  },
  "devDependencies": {
    "cfx.rollup-plugin-coffee2": "^0.0.3",
    "coffee-require": "^0.0.3"
  }
}
